{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-secret",
  "description": "Bundled local-only secret crypto runtime.",
  "windows": ["secret"],
  "local": true,
  "permissions": ["core:default", "allow-stem-secret-commands"]
}
