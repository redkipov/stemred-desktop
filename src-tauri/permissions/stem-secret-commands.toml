"$schema" = "../gen/schemas/desktop-schema.json"

[[permission]]
identifier = "allow-stem-secret-commands"
description = "Allows only the bundled secret runtime to query native crypto readiness."
commands.allow = ["secret_crypto_status"]
